
    // it('delete-usre', () => {
    //   cy.visit(`${client}/signin`);
    //   cy.wait(3000);
    //   cy.origin('http://localhost:3000/admin/dashboard', () => {
    //   cy.get('ul[role="menu"] li:nth-child(1) a').click();
    //   })
      
    // })
    




    //    cy.wait(3000); // Waits for 3 seconds
    //    cy.origin('https://clasor-sandbox.pod.ir/admin/repositories', () => {
    //    cy.contains('span', 'دسته بندی جدید').parent('button.dialog-content__button').click(); 

    //    cy.wait(3000); // Waits for 3 seconds

    //    const uuid = () => Cypress._.random(0, 1e6);
    //    const id = uuid();12
    //    cy.get('[id="category-create-title"]').type(`test_${id}`);
    //    cy.get('[id="category-create-order"]').type("123");
    //    cy.get('[id="category-create-description"]').type("123");
    //    cy.get('.edit-category__form > .modal-action > .modal-btn-success').click();
    //    cy.wait(3000); // Waits for 3 seconds
    //    cy.get('table > tbody > tr:nth-child(1)').click({ force: true });
    //   });
   
    // })
    //   cy.origin('http://sso-sandbox.pod.ir', () => 
             // Inside callback baseUrl is https://example.cypress.io
    //         cy.visit('/oauth2/authorize/index.html?client_id=18682629g64434d74b0004e8ecb3d3be1&response_type=code&redirect_uri=http://localhost:3000/signin&scope=profile+social_write+social');
    //         cy.wait(5000);
    //         cy.get('#authSelAccBtn').click();
    //         cy.get('#authIdentity-inp').type('eli69');
    //         cy.get('#authPassword-inp').type('HQ[>684ngg');
    //         cy.get('#authLoginBtn').click();
    //       })
    // cy.get('.btn.no-background.current').first().click();

    // it('creatdocument', () => {
    //       cy.wait(3000); // Waits for 3 seconds
    //       cy.origin('https://clasor-sandbox.pod.ir/admin/repositories', () => {
    //       cy.contains('span', 'سند جدید').parent('button.dialog-content__button').click(); 
    //       cy.wait(3000); // Waits for 3 seconds
    //       const uuid = () => Cypress._.random(0, 1e6);
    //       const id = uuid();12
    //       cy.get('[id="document-create-title"]').type(`test_${id}`);
    //       cy.get('[id="document-create-type"]').type(`test_${id}`);
    //       cy.get('[id="document-create-number"]').type(`test_${id}`);
    //       cy.get('[id="document-create-order"]').type("123");
    //       cy.get('[id="document-create-description"]').type("123");
    //       cy.get('.edit-document__form > .modal-action > .modal-btn-success').click();

    //      });


    // cy.visit('http://sso-sandbox.pod.ir/oauth2/authorize/index.html?client_id=18682629g64434d74b0004e8ecb3d3be1&response_type=code&redirect_uri=http://localhost:3000/signin&scope=profile+social_write+social');
    // cy.get('#authSelAccBtn').click();
    // cy.get('#authIdentity-inp').type('eli69');
    // cy.get('#authPassword-inp').type('HQ[>684ngg');
    // cy.get('#authLoginBtn').click();
    // //cy.get('.btn.no-background.current').first().click(); 
    // cy.get('.notice').should('be.visible');
      // cy.get('.notice button').click();


    //cy.get('button.w-fit').click();
      //cy.get('td div.flex felx-wrap button:first').click();
      
 
      // cy.wait('@signin', {
      //   timeout: 30000
      // })




      //  cy.get('[id="document-create-type"]').type(`test_${id}`);
    //  cy.get('[id="document-create-number"]').type(`test_${id}`);


    //cy.get('.edit-document__form > .modal-action > .modal-btn-success').click();


    
      // cy.get('.dropdown > .btn').click();
      // cy.contains('ساخت زیر دسته بندی').click();
  
  
      
      // cy.wait(3000); // Waits for 3 seconds
      // const childId = uuid();
      // cy.get("#category-create-title").type(`test_${childId}`);
      // cy.get("#category-create-order").type("42");
      // cy.get("#category-create-description").type("This is a test category.");
      // cy.wait(3000); // Waits for 3 seconds
      // cy.get('[ data-cy="create-subcat-btn"]').click();
      // cy.wait(3000); // Waits for 3 seconds
   
  



          // it("createcategory", () => {
    //   cy.wait(3000); // Waits for 3 seconds
    //   cy.intercept("GET", `${server}/v1/auth/getMe`).as("signin");
    //   cy.visit("http://localhost:3000/admin/repositories");
    //   cy.wait("@signin", {
    //     timeout: 60000,
    //   });
  
    //   cy.get('.createMenu').click();
    //   cy.get('.create-category').click();
  
    //   cy.wait(3000); // Waits for 3 seconds
  
    //   const uuid = () => Cypress._.random(0, 1e6);
    //   const parentId = uuid();
    //   cy.get('[id="category-create-title"]').type(`test_${parentId}`);
    //   cy.get('[id="category-create-order"]').type("123");
    //   cy.get('[id="category-create-description"]').type("123");
    //   cy.get(".edit-category__form > .modal-action > .modal-btn-success").click();
    //   cy.wait(3000); // Waits for 3 seconds
    // });

   //cy.get('.some-element').should('be.visible');


    //cy.get('div[data-cy="document-delete"]').click();
     //cy.get('[data-cy="document-delete"]').click(); //cy.get('table > tbody > tr:nth-child(1)').click({ force: true });


    //cy.get('tbody tr.cursor-pointer').second().click();   // Select the rows in the table
    //cy.get('table > tbody > tr:nth-child(1)').click({ force: true });
    //cy.contains('بعدی').click()
    
    //cy.get('.category-breadcrumb button.block').click();

    //cy.get('button').click();



    //  it('Transfer-Cat', () => {
//   cy.visit('http://localhost:3000/admin/repositories');
//   cy.get('li').eq(1).click();
//   cy.wait(3000); // Waits for 3 seconds
//   cy.get('table > tbody > tr:nth-child(1)').click({ force: true });
//   cy.wait(3000); // Waits for 3 seconds
//   cy.get('.cursor-pointer').eq(1).find('.dropdown > .btn').click();

//   // cy.get('.category-action-transfer').click()

//  });
    //  cy.get('.category-action-transfer').click();
    //  cy.get('.category-action-transfer').click();


    //cy.get('div[role="button"][tabindex="0"]').click();

    //cy.get('div[role="button"][tabindex="0"]').contains('مخفی سازی').click({ force: true });



    
    //cy.get('#headlessui-combobox-input-15').type('test')




    //cy.contains('#headlessui-combobox-options-33 li', 'test').click()
    //cy.contains('#headlessui-combobox-options-33 li', 'test').click()

    // cy.get('.modal-box .custom-table tbody tr.cursor-pointer').first().click();
    // cy.wait(3000); // Waits for 3 seconds
  



    // it('publicversion', () => {
//   cy.get('[data-cy="document-menu-btn"]').click();
//   cy.get('[data-cy="dropdown-version-btn"]').click();
//   cy.wait(3000); // Waits for 3 seconds
//   cy.get('[data-cy="dropdown-right-versionCypress-1"]').click();
//   cy.get('[data-cy="sendPublic"]').click();
//   cy.get('button.public-version--success').click();
//   cy.wait(9000); // Waits for 3 seconds
//   cy.get('button.w-fit.mr-5 > svg').click();
// });


//cy.get('.edit-category__form > .modal-action > .modal-btn-success').as('submitBtn')
// cy.get('@submitBtn').click();
// cy.wait(3000); // Waits for 3 seconds
// cy.get('@submitBtn').should('have.attr', 'data-success').and('equal', 'false');



// console.log('****')
//   console.log(data)
// console.log('****')
// console.log('****')
//   console.log(repositoryId)
// console.log('****')














  // Assuming the response body is an object with a property that contains the repoId
  // const repositoryId = response.body.data.list[0].id; // Adjust the property path as needed

  // Store the ID in the fixture file
  // cy.writeFile('cypress/fixtures/repositoryId.json', { id: repositoryId });

  // Proceed with the test using the stored repository ID
 // cy.fixture('repositoryId').then((data) => {
 // const repoId = data.id; // Use the repository ID from the fixture
//cy.get('table > tbody > tr:nth-child(1)').click({ force: true }); // });














MOhseeeeeeeeeeeeeeeeeeeeeeeen




// Cypress.Commands.add("CreateRepo", () => {
//   cy.wait(5000);
//   cy.intercept("GET", `${server}/v1/auth/getMe`).as("signin");
//   cy.visit("http://localhost:3000/admin/dashboard");
//   cy.intercept('GET', `${server}/v1/auth/getMe`).as('signin');
//   cy.wait("@signin", {
//     timeout: 6000,
//   });

//   // add interceptor for creating repository
//   cy.intercept('POST', `${server}/v1/repositories`).as('createRepo');

//   cy.wait(3000); // Waits for 3 seconds
//   cy.contains("مخزن جدید").click();
//   const uuid = () => Cypress._.random(0, 1e6);
//   const id = uuid();
//   cy.get('[id="repo-name"]').type(`test_${id}`);
//   cy.get('[id="create-repo-description"]').type(`test_${id}`);
//   cy.get(".btn.craete-repo--success").click();

//   cy.wait('@createRepo',{ timeout: 10000 }).then((interception) => {
//     expect(interception.response?.statusCode).to.equal(200);
//     console.log("id =>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>", interception.response);
//   });
// });

mohseeeeeeeeeeeeeeeeeeeeeeeeeeeeeeen










// const getIframeDocument = () => {
//   return cy
//   .get('iframe[data-cy="remote-editor"]')
//   // Cypress yields jQuery element, which has the real
//   // DOM element under property "0".
//   // From the real DOM iframe element we can get
//   // the "document" element, it is stored in "contentDocument" property
//   // Cypress "its" command can access deep properties using dot notation
//   // https://on.cypress.io/its
//   .its('0.contentDocument').should('exist')
// }

// const getIframeBody = () => {
//   // get the document
//   return getIframeDocument()
//   // automatically retries until body is loaded
//   .its('body').should('not.be.undefined')
//   // wraps "body" DOM element to allow
//   // chaining more Cypress commands, like ".find(...)"
//   .then(cy.wrap)
// }


  // cy.get('div#cke_1_contents iframe').then($iframe => {
  //   // Get the iframe's document
  //   const iframeDoc = $iframe.contents().get(0)

  //   // Find the element within the iframe you want to edit
  //   const elementWithinIframe = iframeDoc.querySelector('your_selector_here')

  //   // Edit the content (for example, changing the text)
  //   elementWithinIframe.textContent = 'Your new content here'

  //   // Assert that the content has been edited as expected
  //   expect(elementWithinIframe.textContent).to.equal('Your new content here')
  // })


    // cy.get('.cke_wysiwyg_frame').then($iframe => {
    //   // Get the body of the rich text editor inside the iframe
    //   const $body = $iframe.contents().find('body');

    //   // Type text into the rich text editor
    //   cy.wrap($body).type('This is a test text entered by Cypress!');

    //   // Save the content (you need to replace this with your save functionality)
    //   cy.get('button.save-button').click(); // Replace 'button.save-button' with your actual save button selector
    // });

    // getIframeBody().find('body[contenteditable="true"]').click()
    // cy.get('iframe[data-cy="remote-editor"]', { timeout: 200000 }).should('be.visible').then($iframe => {
    //   // Switch to the iframe context
    //   const $iframeBody = $iframe.contents().find('body');

    //   // Type text into the CKEditor iframe body
    //   cy.wrap($iframeBody).type('Hello, this text is entered using Cypress!');
    // });
    // cy.wait(10000);

    // cy.get('iframe.cke_wysiwyg_frame').then($iframe => {
    //   // Switch to the iframe context
    //   const iframeContent = $iframe.contents();

    //   // Select the element inside the iframe by its ID
    //   cy.wrap(iframeContent.find('#cke_72')).should('have.text', 'کلید Alt+0 را برای راهنمایی بفشارید');
    // });

    //cy.get('div.main-tabs__tab-content table tbody tr:nth-child(1) td.actions div ul div li:nth-child(4)').click({ force: true });
    //cy.contains('ویرایش').click();
    //cy.get('[data-cy="submit-createversion-btn"]').click(); 



    // it('createversion-and-Copy-Address', () => {
  //   cy.wait(5000);
  //   cy.get('[data-cy="document-menu-btn"]').click({ force: true });
  //   cy.get('[data-cy="dropdown-version-btn"]').click({ force: true });
  //   cy.wait(3000); // Waits for 3 seconds
  //   cy.get('[data-cy="dropdown-right-versionCypress-0"]').click({ force: true });
  //   cy.contains('کپی آدرس اشتراک‌ گذاری').click();
  //   //cy.get('div.main-tabs__tab-content table tbody tr:nth-child(0) td.actions div ul div li:nth-child(6)').click({ force: true });
  //   cy.on('window:confirm', () => {
  //     return true; // Confirm the prompt
  //   });
  //  });


      //cy.get('iframe[title="remote editor"]', { timeout: 10000 }).should('be.visible').then($firstIframe => {
    // Switch to the first iframe context
    //   const $firstIframeContent = $firstIframe.contents();

    //   // Wait for the second iframe inside the first iframe to become available
    //   cy.wrap($firstIframeContent.find('iframe')).should('be.visible').then($secondIframe => {
    //     // Switch to the second iframe context
    //     const $secondIframeContent = $secondIframe.contents();

    //     // Find the body tag inside the second iframe
    //     const $body = $secondIframeContent.find('body');

    //     // Type text into the body tag inside the second iframe
    //     cy.wrap($body).type('Hello, this text is entered using Cypress!');
    //   });


    // cy.wait(3000);

    // cy.get('[data-cy="sendPublic"]').click({ force: true });
    // cy.wait(3000); // Waits for 3 seconds
    // cy.get('button.public-version--success').click({ force: true });
    // cy.wait(9000); // Waits for 3 seconds
    // cy.get('button.w-fit.mr-5 > svg').click({ force: true });







    // cy.get('[data-cy="submit-version-menu-btn"]').click({ force: true });
    // cy.get('button.w-5.h-5.mr-4').click();
    // cy.get('button.w-fit.mr-5').click();

    //cy.get('[data-cy="submit-version-menu-btn"]').click({ force: true });
    //cy.wait(4000);
    //cy.get('[data-cy="close-version-menu-btn"]').click({ force: true });
